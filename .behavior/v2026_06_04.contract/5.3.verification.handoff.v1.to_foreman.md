# handoff: integration test credentials

## blocker

integration tests require `SDK_AWS_S3_TEST_BUCKET` environment variable, which is not configured.

## what i tried

1. **ran test suites** — types, lint, format, unit all pass
2. **checked keyrack** — `.agent/keyrack.yml` shows `env.test: null` (not filled)
3. **reviewed test setup** — `src/__test_assets__/getTestBucket.ts` throws if env var absent

## why this is foreman-only

the keyrack credential `env.test` must be filled by the foreman. mechanics cannot fill keyrack credentials — only unlock them after they're filled.

## current test status

| suite | command | result |
|-------|---------|--------|
| types | `rhx git.repo.test --what types` | passed |
| lint | `rhx git.repo.test --what lint` | passed |
| format | `rhx git.repo.test --what format` | passed |
| unit | `rhx git.repo.test --what unit --mode apply` | 7 tests passed |
| integration | `rhx git.repo.test --what integration --mode apply` | blocked — SDK_AWS_S3_TEST_BUCKET not set |
| acceptance | (not yet run) | blocked — depends on integration |

## what foreman needs to do

1. fill the keyrack credential:
   ```sh
   rhx keyrack fill --owner ehmpath --env test
   ```
   set `SDK_AWS_S3_TEST_BUCKET` to a valid S3 bucket name

2. rewind and continue:
   ```sh
   rhx route.stone.set --stone 5.3.verification --as rewound
   ```

## note

the integration tests are real S3 integration tests (no mocks). they require a bucket that:
- exists in AWS
- is accessible with test credentials
- allows put, get, delete operations

the 4 integration test files verify:
- `getOne.integration.test.ts` — get object, 404 handle
- `getAll.integration.test.ts` — list and fetch with prefix
- `set.integration.test.ts` — put object, overwrite
- `del.integration.test.ts` — delete object, idempotency
