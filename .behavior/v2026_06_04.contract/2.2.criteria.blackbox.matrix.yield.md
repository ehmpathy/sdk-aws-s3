# blackbox criteria matrix: sdk-aws-s3

## matrix.1 = get.one

| ind: input format | ind: object state | dep: return value | dep: error |
|-------------------|-------------------|-------------------|------------|
| URI | exists | string content | none |
| URI | not found | null | none |
| URI | bucket absent | — | thrown with bucket context |
| bucket+key | exists | string content | none |
| bucket+key | not found | null | none |

**gaps**: none — all combinations covered

---

## matrix.2 = get.all

| ind: input format | ind: object count | dep: return value | dep: pagination |
|-------------------|-------------------|-------------------|-----------------|
| URI | multiple | Array<{ key, content }> | internal |
| URI | zero | [] | n/a |
| URI | >1000 | all objects | internal |
| bucket+prefix | multiple | Array<{ key, content }> | internal |

**gaps**: none — all combinations covered

---

## matrix.3 = set

| ind: input format | ind: object state | dep: action | dep: return |
|-------------------|-------------------|-------------|-------------|
| URI | not exists | create | void |
| URI | exists | overwrite | void |
| bucket+key | not exists | create | void |
| bucket+key | exists | overwrite | void |

**gaps**: none — all combinations covered

---

## matrix.4 = del

| ind: input format | ind: object state | dep: action | dep: return |
|-------------------|-------------------|-------------|-------------|
| URI | exists | delete | void (silent) |
| URI | not found | no-op | void (silent) |
| bucket+key | exists | delete | void (silent) |
| bucket+key | not found | no-op | void (silent) |

**gaps**: none — all combinations covered

---

## matrix.5 = URI parse

| ind: URI format | dep: parse result | dep: error |
|-----------------|-------------------|------------|
| valid s3://bucket/key | bucket + key extracted | none |
| invalid scheme (http://) | — | thrown with message |
| malformed (s3://) | — | thrown with message |

**gaps**: none — all combinations covered

---

## matrix.6 = context injection

| ind: context provided | dep: client used |
|-----------------------|------------------|
| custom S3Client | custom client |
| none | default S3Client |

**gaps**: none — all combinations covered

---

## matrix.7 = error propagation

| ind: error type | dep: behavior |
|-----------------|---------------|
| AccessDenied | propagate with context |
| network error | propagate with context |
| throttle error | propagate with context |

**gaps**: none — all combinations covered

---

## decomposition assessment

all matrices have 2-3 independent dimensions — within acceptable bounds.

no decomposition needed.
