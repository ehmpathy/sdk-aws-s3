# roadmap: sdk-aws-s3

## phase 0: foundation

### 0.1 context type

- [ ] create `src/contract/types.ts`
  - [ ] export `ContextSdkAwsS3` type
  - [ ] shape: `{ aws?: { s3?: { client?: S3Client } } }`

**verify**: types compile

---

### 0.2 transformer: asS3Ref

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 43-46

- [ ] create `src/domain.operations/asS3Ref.ts`
  - [ ] input: `{ uri }` or `{ bucket, key }`
  - [ ] output: `{ bucket: string; key: string }`
  - [ ] parse URI format `s3://bucket/path/to/key`
  - [ ] throw on invalid scheme (not `s3://`)
  - [ ] throw on malformed URI (no bucket or key)

- [ ] create `src/domain.operations/asS3Ref.test.ts`
  - [ ] positive: valid URI → parsed bucket + key
  - [ ] positive: bucket+key passthrough
  - [ ] negative: invalid scheme throws
  - [ ] negative: malformed URI throws
  - [ ] edge: empty key
  - [ ] edge: key with trailing slash

**verify**: `rhx git.repo.test --what unit --scope asS3Ref --mode apply`

---

## phase 1: core operations

### 1.1 getOne

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 48-53

- [ ] create `src/domain.operations/getOne.ts`
  - [ ] call asS3Ref to parse input
  - [ ] call S3Client.send(GetObjectCommand)
  - [ ] handle 404 → return null (dual check: name + httpStatusCode)
  - [ ] return string content

- [ ] create `src/domain.operations/getOne.integration.test.ts`
  - [ ] positive: object exists → content returned
  - [ ] negative: object absent → null
  - [ ] negative: bucket absent → error
  - [ ] edge: key with special chars

**verify**: `rhx git.repo.test --what integration --scope getOne --mode apply`

---

### 1.2 set

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 63-66

- [ ] create `src/domain.operations/set.ts`
  - [ ] call asS3Ref to parse input
  - [ ] call S3Client.send(PutObjectCommand)
  - [ ] return void

- [ ] create `src/domain.operations/set.integration.test.ts`
  - [ ] positive: create new object
  - [ ] positive: overwrite extant object
  - [ ] negative: bucket not found → error
  - [ ] edge: empty body

**verify**: `rhx git.repo.test --what integration --scope set --mode apply`

---

### 1.3 del

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 68-71

- [ ] create `src/domain.operations/del.ts`
  - [ ] call asS3Ref to parse input
  - [ ] call S3Client.send(DeleteObjectCommand)
  - [ ] return void (idempotent)

- [ ] create `src/domain.operations/del.integration.test.ts`
  - [ ] positive: delete extant object
  - [ ] negative: delete absent object → silent success (idempotent)

**verify**: `rhx git.repo.test --what integration --scope del --mode apply`

---

## phase 2: composite operation

### 2.1 getAll

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 55-61

- [ ] create `src/domain.operations/getAll.ts`
  - [ ] call asS3Ref to parse input
  - [ ] loop: S3Client.send(ListObjectsV2Command)
  - [ ] handle pagination via ContinuationToken
  - [ ] for each key: call getOne
  - [ ] return Array<{ key, content }>

- [ ] create `src/domain.operations/getAll.integration.test.ts`
  - [ ] positive: multiple objects → array returned
  - [ ] negative: empty prefix → empty array
  - [ ] edge: >1000 objects (pagination)

**verify**: `rhx git.repo.test --what integration --scope getAll --mode apply`

---

## phase 3: contract

### 3.1 sdk export

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 36-41

- [ ] create `src/contract/index.ts`
  - [ ] export `sdkAwsS3` object
  - [ ] wire `get.one` → getOne
  - [ ] wire `get.all` → getAll
  - [ ] wire `set` → set
  - [ ] wire `del` → del

- [ ] create `src/index.ts`
  - [ ] re-export from `src/contract/index.ts`

**verify**: types compile, exports work

---

## phase 4: acceptance

### 4.1 operation snapshots

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 126-147

- [ ] create `src/contract/index.acceptance.test.ts`
  - [ ] snapshot: getOne return value (string)
  - [ ] snapshot: getOne return value (null for absent)
  - [ ] snapshot: getAll return value (array shape)
  - [ ] snapshot: set return value (void)
  - [ ] snapshot: del return value (void)
  - [ ] snapshot: invalid URI scheme error
  - [ ] snapshot: malformed URI error
  - [ ] snapshot: bucket not found error

**verify**: `rhx git.repo.test --what acceptance --mode apply`

---

### 4.2 journey test

**read before**:
- `.behavior/v2026_06_04.contract/3.3.1.blueprint.product.yield.md` lines 142-147

- [ ] create `src/contract/index.journey.acceptance.test.ts`
  - [ ] [t0] object absent (null)
  - [ ] [t2] object created (content)
  - [ ] [t4] object updated (content)
  - [ ] [t5.5] invalid URI error (error message)
  - [ ] [t7] object deleted (null)

**verify**: `rhx git.repo.test --what acceptance --mode apply`

---

## verification summary

| phase | verify command |
|-------|----------------|
| 0.2 | `rhx git.repo.test --what unit --scope asS3Ref --mode apply` |
| 1.1 | `rhx git.repo.test --what integration --scope getOne --mode apply` |
| 1.2 | `rhx git.repo.test --what integration --scope set --mode apply` |
| 1.3 | `rhx git.repo.test --what integration --scope del --mode apply` |
| 2.1 | `rhx git.repo.test --what integration --scope getAll --mode apply` |
| 3.1 | `rhx git.repo.test --what types` |
| 4.1-4.2 | `rhx git.repo.test --what acceptance --mode apply` |

---

## dependency graph

```
phase 0.1 (types)
    │
    v
phase 0.2 (asS3Ref) ─────────────────────┐
    │                                     │
    ├──────────┬──────────┐              │
    v          v          v              │
phase 1.1   phase 1.2   phase 1.3        │
(getOne)    (set)       (del)            │
    │                                     │
    v                                     │
phase 2.1 (getAll) ──────────────────────┤
    │                                     │
    v                                     │
phase 3.1 (contract) ────────────────────┤
    │                                     │
    v                                     v
phase 4.1-4.2 (acceptance) ──────────────┘
```
