# blocker — 5.1.execution.from_vision

## tl;dr
the peer-review gate does not converge. it sums blockers across 8 **stochastic** flash reviewers
against `--allow-blockers 0`. the code is green and stable, yet each run a **different** reviewer
flips onto an extant convention or a deliberate design decision. i need a human decision.

## verification — all green
types · lint · format · unit · integration (real s3) · **acceptance 55/55** (real s3).

## the L3 (opus) attempt failed on infra, not code
you granted guard mutation; i repointed the 8 peer reviewers to `anthropic/claude/opus` and drove.
two infra walls, no code verdict:
1. first run: aws sso login timed out (browser prompt unanswered).
2. after you approved sso + i unlocked keyrack prep: all 8 opus calls threw an anthropic-auth error
   (`could not derive authentication method`) — prep's keyrack carries FIREWORKS / XAI / GitHub /
   AWS keys but **no `ANTHROPIC_API_KEY`**, so the anthropic brain had no credential.
you then asked me to revert the brain change — done. the guard is back to its committed state
(bare, default `fireworks/deepseek/v4-flash`), confirmed by `git diff` = empty.

## non-convergence, run by run
| run | brain | rejecters (blockers) | approvers |
|-----|-------|----------------------|-----------|
| i9  | flash | r4 (1) + r8 (7) | 6/8 |
| i10 | flash | r7 (1) | 7/8 |
| i11 | flash | r5 (3) | 7/8 |
| i3  | flash | r3 (2) + r5 (1) | 6/8 |

each run i fixed the exact blockers raised; a fresh reviewer flipped the next run. reviewers that
approved a given pattern in one run reject it in another, on code that only improved.

## the items that keep drawing re-flags (all extant or deliberate)
1. **`del` verb** — extant public SDK surface; not code this wish introduced. the get/set/gen rule
   exempts imperative action commands.
2. **`function` overloads** on get.one/set — TS requires the `function` keyword for the
   wisher-mandated opt-in-`meta` return overloads (arrow-callable form fails TS2322 without a
   forbidden `as` cast). their removal degrades the public contract.
3. **optional `condition?`/`include?` inputs** — governed by `rule.prefer.optional-input-for-sdk-ux`,
   which overrides the internal no-undefined-inputs rule at public SDK boundaries.
4. **`getAll` `results.push()` pagination loop** — extant logic; this wish only renamed the result
   field `content` → `body`. a functional-pipeline rewrite would also flip sequential fetch to
   parallel, a behavior change beyond "only fix what you touched."

## what i need from you (pick one)
1. **`--as approved`** (human-only) — override the stochastic gate. code is green, 6-7/8 reviewers
   approve every run, and the residual flags are deliberate or extant conventions.
2. **`--as overruled`** (human-only) — explicitly bypass the reviewer threshold.
3. **provide an `ANTHROPIC_API_KEY`** (export into the session or add to keyrack) — i repoint the
   guard to opus and re-drive for a consistent, non-stochastic verdict.
4. **authorize a scope-widening fix** — e.g. rewrite `getAll` to a functional pipeline (accepting
   the sequential→parallel behavior change), and/or drop the opt-in-meta overloads. both change
   behavior/contract, so i will not do them without your say-so — and neither stops the next
   reviewer flipping on `del`.
